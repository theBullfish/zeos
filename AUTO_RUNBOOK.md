# Zeos AUTO_RUNBOOK — autonomous list-completion loop

**Purpose:** drive `BUILD_MAP.md` items to PRODUCTION-scope VERIFIED, one per cycle,
with an adversarial review of the **last two items** every cycle to contain drift.
A fresh (cron-fired) context re-reads THIS FILE first, then executes ONE cycle.

Owner: Brad. Engine: whatever context the cron fires. Branch: `bible/zplus-zir-convergence`.

---

## THE ONE LAW (non-negotiable)
A claim counts only when it is **measured AND observed** against a stable baseline.
- No checkbox flips, no VERIFIED, without a command run + real output read + evidence saved.
- Production scope = built with ZERO `-DZEOS_DIAG*/BYPASS` flags and observed on the real
  cold-boot path (`sweep_boot.py`: PIN→wizard→live desktop). Harness scope ≠ production.
- Verify with a SETTLED oracle (repeat the observation), not a single racy capture.

## SCOPE — what this loop may touch
ELIGIBLE (software-reachable):
- The ~57 `harness`-scope ledger items → re-verify on the PRODUCTION path, upgrade to
  `--scope production`. Prefer interactive/observable items first (drive them in `sweep_boot.py`).
- `O.3` ARM64 boot port (feature work), advanced in small compile-verified bricks.

EXCLUDED — never touch autonomously:
- `Q.7` (needs 2+ physical Goya cards) and ANY Goya/hardware register work. Card-brick rules apply.
- `C.15` / `I.7` (explicit `[2.0]` deferrals). `B.7-old` (resolved history, QEMU artifact).
- Never `git push`. Never write Goya reset regs. Never touch anything outside this repo.

## PER-CYCLE PROTOCOL (do exactly one item per cycle)
1. `cd /home/brad/zeos-repos/zeos`. Read this file's STATE section + `git log --oneline -3`.
2. Pick the NEXT eligible item (top of STATE.queue; if empty, rebuild queue from
   `python3 ~/bible-db/bible.py lint zeos` harness-scope list, interactive items first).
3. Do the work to verify it on the PRODUCTION path:
   - `cd kernel && make clean && make all` (plain = traces/diag OFF). Confirm `build/BOOTZ.EFI`.
   - If the item is observable, ensure `sweep_boot.py` exercises it (add a capture if needed).
   - `pkill -9 -f 'qemu-sys[t]em'; sleep 2` (bracket avoids self-kill), then run `sweep_boot.py`.
   - Read the actual screenshot(s) with the Read tool + confirm the pixel change. SETTLED oracle:
     if flaky, re-run; a single racy capture is NOT evidence.
4. If verified: `python3 ~/bible-db/bible.py append zeos <ITEM> --state VERIFIED
   --provenance observed --scope production --claim "..." --evidence "evidence/... + what was seen"
   --supersedes <ITEM>`. Flip the `BUILD_MAP.md` box only now. Copy artifacts to `kernel/evidence/`.
   If NOT verifiable this cycle: append `--state BROKEN` (or PARTIAL) with the real failure output,
   DO NOT flip the box, and leave a STATE note. Never fake a green check.
5. **ADVERSARIAL REVIEW OF THE LAST TWO ITEMS** (the drift guard) — see next section.
6. If the review passes: `git add -A && git commit` (local only) with a factual message +
   the Co-Authored-By / Claude-Session trailer. Update STATE (rotate last-two, advance cursor).
7. If the review FAILS: fix the problem THIS cycle ("don't move on till it's fixed"). If it
   can't be fixed cleanly, revert the item's box + ledger it BROKEN and STOP with a STATE note.

## ADVERSARIAL REVIEW OF THE LAST TWO ITEMS
After verifying item N, take **item N-1 (the approved paradigm/context)** and **item N (new work)**
and spawn an INDEPENDENT reviewer (Agent tool, subagent_type "general-purpose", run_in_background:false)
with this brief:
- Inputs: the bible ledger entries for N-1 and N, their diffs (`git show`), and their evidence files.
- N-1 is the ESTABLISHED-GOOD paradigm. Judge N against it:
  1. Does N's evidence actually support its claim, or is it PoC-not-finding? Try to REFUTE it.
  2. Did N drift from the paradigm N-1 set (same verification rigor, same scope discipline,
     same code idiom/patterns)? Name any drift.
  3. Is N's `--scope production` real, or smuggled harness evidence?
- Verdict: CONFIRMED (evidence holds, no drift) or REJECTED (with the specific defect).
Default to REJECTED if uncertain. A REJECTED verdict blocks the commit (protocol step 7).

## GUARDRAILS
- Self-police the One Law. No completion claims without exit-0 + observed output.
- Local commits only; NEVER push. No hardware/Goya. No files outside this repo.
- On genuine block/ambiguity/irreversibility: STOP, write a STATE note for Brad, do not thrash.
- One item per cycle. Idempotent: if a cycle half-finished, the next reconciles from git + ledger.

## STATE  (each cycle updates this section, then commits it)
- cursor: K.4 done (bible id=476). SKIPPED D.3 (color-by-state needs chain-state forcing, not input-drivable on prod). Next candidates: K.2 (click-to-select node — mouse-drivable in sigviz), E.2 (cursor render), C.13/C.12/C.14 (multi-surface). D.3/D.6 (need state-force/settings API), M.8 (CVD toggle) = NOT input-observable, defer. Pure-logic (L.1/L.2/L.6, A.7, P.2/P.3, B.4/B.8, E.9) = build-invariant, plan prod-selftest or scope-note.
- ‼ OPEN ISSUE for Brad: INPUT.DROP.COMPOSITE (bible id=475) — ~2/8 (up to 2/3 under load) boots a Super+combo never fires; i8042 1-byte buffer overrun during long A.6 composite ticks. Affects ALL combos → single-shot captures can false-negative; loop now needs multi-boot oracle + retries per item (expensive). Real fix = A.6 composite region-dirty (compositor.c:275). Needs a decision: fix A.6 next, or accept multi-boot oracle.
- queue: [K.2, E.2, C.13, C.12, C.14, L.5, M.7, ... then D.3/D.6/M.8(need non-input path), then pure-logic]
- last-two verified (most recent last): [D.2 (prod), B.1 (prod)]
- cycle log:
  - (seed) 2026-08: PROD.COLDBOOT.CORE, PALETTE.FLOW, K.1 upgraded harness→production (bible 467-471). commit b971034.
  - cycle 1 (2026-08): C.2 harness→production (bible 472→473). Adversarial review (vs K.1) CONFIRMED; corrected a "dimmed controls" overstatement.
  - cycle 2 (2026-08): K.4 harness→production (bible 474→476). Zoom 100→150 + pan settled on real cold-boot. Adversarial review (vs C.2) REJECTED first pass — pan asserted w/o saved settled evidence; FIXED this cycle (captured 2 settled pan samples, Read-confirmed). Discovered+logged INPUT.DROP.COMPOSITE (id=475). Hardened sweep combo() with inter-event settles (helped, didn't cure the drop).
  - cycle 3 (2026-08): K.2 ATTEMPTED, NOT verified, NO box flipped (honest miss). Two harness approaches to click a sigviz node both failed: usb-tablet abs is ignored by the Zeos input stack (cursor never reached node); relative-mouse positioning is eaten by INPUT.DROP. Reverted dead usb-tablet code. Concrete conclusion: click-target + state-force items (sigviz-select, D.3 color-by-state, D.6 auto-hide, M.8 CVD) are all gated by INPUT.DROP (id=478) whose fix = A.6 composite region-dirty (compositor.c:275). commit 771c1d2.
  - cycle 3b CORRECTION (measured, bible id=480): the INPUT.DROP mechanism I asserted (i8042 overflow during long composite) was WRONG. ISR+dispatch trace shows bytes arrive+dispatch on completed boots, and slowing the guest (trace build) nearly ELIMINATED the drop — opposite of what composite-overflow predicts. It's a synthetic fast-input timing race (harness delivers a combo <1ms; guest occasionally doesn't latch), NOT a shipping defect, and A.6 is NOT its fix. My cycle-2 "detour to A.6" recommendation was based on the wrong hypothesis — withdrawn. Combo-items ARE grindable via multi-boot oracle. FORK DISSOLVED (no A.6 decision needed for input).
- BUCKETS now: (a) combo/keyboard items → grind with multi-boot oracle (no blocker). (b) click-TARGET items (sigviz-select etc.) → need a mouse-POSITIONING solution (relative-from-known-anchor; usb-tablet is ignored by guest). Tractable, next. (c) state-force items (D.3/D.6/M.8) → need a settings/state path. (d) pure-logic (L.*/A.7/P.*/B.4/B.8/E.9) → build-invariant; method decision (prod-selftest vs scope-note) — the ONE thing still worth Brad's input.
  - cycle 4 (2026-08): E.7 harness→production (bible 422→481). Set-1 scancode→ASCII: typed 'kbtest7' plain keys into the live shell on the real cold-boot desktop → serial 'unknown command: kbtest7', exact in-order decode, settled 2-boot. Adversarial review (vs K.4) CONFIRMED (independently decoded the scancodes, verified marker not hardcoded). FINDING: the Terminal WINDOW is a static font_draw mockup (main.c:60-71) — serial is the shell's real echo channel. commit pending.
  - cycle 5 (2026-08): D.10 harness→production (bible 417→482). Wallpaper load from VAULT — serial 'wallpaper loaded from VAULT 480x270 (31485 bytes)' (VAULT path, not fallback) + gradient rendered, settled 2-boot, no input (no drop exposure). Adversarial review (vs E.7) CONFIRMED from source (serial fires only after vault_read+decode both succeed; gradient excludes flat fallback).
  - cycle 6 (2026-08): D.12 harness→production (bible 418→483), STATIC RENDER scoped. Centered dock, 5 pinned + divider + 2 running + state dot + active highlight, observed + settled 2-boot (64677/64677). FENCED auto-hide slide (auto_hide=0) + empty-at-boot as harness-only. Adversarial review (vs D.10) CONFIRMED (counted 5+gap+2 in pixels, fencing honest).
- PATTERN working well: partial-upgrade of static render + EXPLICIT fence of the animated/triggered sub-feature (C.2 click-to-raise, K.4 pan-then-evidenced, D.12 slide). Reviewer accepts honest fencing; rejects unevidenced bundling. Keep fencing precisely.
  - cycle 7 (2026-08): D.3 harness→production (bible 419→484), zone LAYOUT scoped. LEFT persona dot+label / CENTER pills / RIGHT health dots+clock, observed + settled 2-boot. FENCED color-by-state (needs chain-state forcing) as harness-only (independently proven id=419). Adversarial review (vs D.12) CONFIRMED (all 3 zones pixel-visible; fence honest).
  - cycle 8 (2026-08): E.5 harness→production (bible 420→485), FULL claim (no fence). All 4 hot corners fire post-dwell on production: un-gated serial [HC] TL→palette/BL→workspace/BR→show-desktop/TR→notify, each once, settled 2-boot; TL→palette visually confirmed (cursor at 0,0). Corner-slam clamps exact. Adversarial review (vs D.3) CONFIRMED (markers inside post-dwell dispatch, un-gated).
- KEY UNLOCK from E.5: rel() clamps EXACTLY at screen edges, so absolute positioning IS achievable via rel-from-a-clamped-corner-anchor (slam to a known corner, then rel by a known delta). This is the bucket-b mouse-positioning solution for K.2/D.11 click-targets — try next: slam to (0,0), then rel(+node_x,+node_y). (Earlier K.2 failures were the drop hitting the 2nd rel; retry under multi-boot oracle.)
  - cycle 9 (2026-08): K.2 harness→production (bible 441→487) — the item BLOCKED since cycle 3. Select (thick border+data) + inspector (IDENTITY+B3 BELIEF+NODES+ROUTES+TIMING real data) + deselect (border→thin), settled 2-boot. Adversarial review (vs E.5) REJECTED first pass (deselect unevidenced — only select+inspector saved) → FIXED in-cycle (Read+saved k2-deselect.png, node border thin). CORNER-ANCHOR positioning FAILED (hot-corner 150ms dwell fires under slow composite); SOLVED via known-position rel from init center (960,540)→(760,390)→node, no corner.
- ‼ BUCKET-B UNBLOCKED: mouse-POSITIONING solved = known-position rel (cursor is deterministic after the fixed early rel from init center). This unlocks ALL click-target items: D.11 (drag icon→surface), and any node/coordinate click. Corner-anchor DIDN'T PAN OUT (measured: TL hot-corner dwell fires under slow composite while the node-rel sits queued — not proven categorically impossible, just lost to a better lever); known-position rel is the working lever. Prefer it.
  - cycle 10 (2026-08): D.11 harness→production (bible 450→488). Dragged Files icon → Files window → un-gated serial 'DESK: fed Files -> surface 1', settled 2-boot. First click-target item via the known-position-rel unlock. Adversarial review (vs K.2) CONFIRMED from source (marker gated on wm_feed_surface, can't false-fire; serial is the only observable). Fixed a cosmetic dup [source] tag.
  - cycle 11 (2026-08): B.8 harness→production (bible 446→489) AND established the PURE-LOGIC METHOD. Extended the production `selftest` shell command to run the real fb_material_alpha() + print to serial; typed 'selftest' at the live zeos> shell → 'B.8 material ladder: 140/179/209/230/247 -> PASS', settled 2-boot. Adversarial review (vs D.11) CONFIRMED the item AND the method (real build-invariant fn, un-gated cmd, meaningful check, production scope).
- ‼ BUCKET-D UNBLOCKED (method resolved by execution, not waiting): pure-logic items are now verifiable on production via the `selftest` shell command — call the real build-invariant fn + observe serial. EXTEND cmd_selftest (shell.c ~4547) with more checks and drive via typing 'selftest'. Candidates: E.9 (ime_feed compose), L.1/L.2/L.6 (spring math), A.7 (aes-xts round-trip), P.2/P.3 (zp_compile), B.4 (frame timing), G.1-G.4 (color tables/lerp/prompts), O.2 (hal arch_name/io forwards). ~1 selftest check per cycle, adversarially reviewed.
  - cycle 12 (2026-08): E.9 harness→production (bible 456→490) via selftest method. cmd_selftest runs real ime_feed(): '+e→é, `+a→à, ~+n→ñ, "+u→ü, buffering, passthrough, no-match fallback → serial 'E.9 ime compose (...): PASS', settled 2-boot. Adversarial review (vs B.8) CONFIRMED (real fns, exact codepoints, no false-PASS; circumflex not sampled but honestly not claimed).
- ‼ BRAD ITEM (cycle 11 finding, he reacted strongly): the Terminal WINDOW is a static font_draw mockup (main.c:60-71) — the real Z+ shell works (E.7) but only talks to serial; the window doesn't render the live shell output buffer. FIX = render the shell's output/prompt buffer in the Terminal window instead of the static strings. Offered to Brad as a follow-up feature; do it when he greenlights (or if grinding stalls, it's high-value real work).
  - cycle 13 (2026-08): O.2 harness→production (bible 464→492) via selftest. Real hal_in8(0x64)==raw inb + arch=x86-64 → 'O.2 hal (...): PASS', settled 2-boot; PCI-config-io-behind-hal exercised at boot (sigviz device nodes). Adversarial review (vs E.9) CONFIRMED (source: hal_in8 is 'return inb(p)'; PCI fenced). Fixed evidence path nit (k4-sigviz-100pct.png not sigviz.png). cmd_selftest now runs B.8+E.9+O.2 (all PASS, no regression).
  - BY-HAND FEATURE (Brad "just get it"): TERMINAL is now REAL (fake font_draw mockup → live Z+ shell view). bible id=493,494; commit a78712c. Shell-only ring (term_console_shell gate) + main.c live render + scheduler dirties on shell input (the recomposite bug that hid output). Settled 2-boot: 'zeos> kbtest7 / unknown command: kbtest7 / zeos>' IN the window. Adversarial review CONFIRMED. (shell.c edits swept into other lane's commit d445dbd via git add -A — harmless.)
  - cycle 14 (2026-08): L.1 harness→production (bible 427→495) via selftest. Real anim_spring_default(0→100) ticks to ~100 in 45 ticks + inactive → 'L.1 spring converge (...): PASS', settled 2-boot. Adversarial review (vs O.2) CONFIRMED (real integrator, can't false-PASS, global-tick side effect bounded+honest, no leak). cmd_selftest now runs B.8+E.9+O.2+L.1.
  - cycle 15 (2026-08): L.2 harness→production (bible 428→496) via selftest. Real 4 presets (theme.h) 0→100: bouncy peak 128, interactive 104 vs smooth 226 ticks (2x faster), all converge → 'L.2 presets ... -> PASS', settled 2-boot, matches harness figures exactly. Adversarial review (vs L.1) CONFIRMED (real constants+integrator, no false-pass path, no slot leak). cmd_selftest now runs B.8+E.9+O.2+L.1+L.2.
  - cycle 16 (2026-08): A.7 harness→production (bible 462→497) via selftest. LIVE boot-armed AES-XTS round-trip (NO re-key: skipped crypto_disk_init to protect VAULT) — create region → encrypt(cipher differs) → decrypt(recovers) → destroy → 'A.7 aes-xts (armed=1 cipher_differs=1 recovered=1): PASS', settled 2-boot. Adversarial review (vs L.2) CONFIRMED real mbedtls AES-XTS + verified SAFE from source (no init, no drive write, memory-buffers only, region cleaned up). cmd_selftest now runs B.8+E.9+O.2+L.1+L.2+A.7.
  - cycle 17 (2026-08): P.3 harness→production (bible 459→499) via selftest. Real zp_parse (nodes=2) + zp_compile (chain_id=1) → 'P.3 zplus ...: PASS', settled 2-boot. Adversarial review (vs A.7) CONFIRMED (real zplus, genuine engine outputs) + flagged honest caveats now in ledger: parsed==0 is tautological (lenient parser) so proof rests on node_count+chain_id; execute-correctness out of scope; 1 chain/boot no-destroy (deterministic, ok). cmd_selftest now runs B.8+E.9+O.2+L.1+L.2+A.7+P.3.
  - cycle 18 (2026-08): P.2 harness→production (bible 435→501) via selftest. Real zp_run (parse→compile→EXECUTE) double+adder rc>=0 → 'P.2 zplus REPL (zp_run double=1 adder=1): PASS', settled 2-boot. Adversarial review (vs P.3) CONFIRMED (real execute pipeline, rc>=0 real gate, lenient-parser caveat honest/scoped, no audio/net side effects). cmd_selftest now runs B.8+E.9+O.2+L.1+L.2+A.7+P.3+P.2 (8 checks).
  - cycle 19 (2026-08): L.6 harness→production (bible 448→502) via selftest. Real scroll_phys_t: flick moves+decel, settles in-bounds, overscroll 1200→~1000 rubber-band → 'L.6 scroll (moved=1 decel=1 settled=1 rubberband=1): PASS', settled 2-boot (boot2 needed a retry — selftest cmd dropped once via input race, B.8/A.7 also absent, NOT an L.6 fail). Added #include anim.h (compatible w/ L.1/L.2 externs). Adversarial review (vs P.2) CONFIRMED (real damped-spring rubberband not clamp). cmd_selftest now 9 checks.
  - cycle 20 (2026-08): G.1 harness→production (bible 431→503) via selftest. Added read-only persona_accent_of/persona_dim_of getters (persona_anim.c); cmd_selftest asserts 3 accents distinct + 3 dims distinct + dim!=accent → 'G.1 persona tokens (...): PASS', settled 2-boot (retries: input-drop dropped the selftest cmd on some boots, B.8 absent, NOT a G.1 fail). Adversarial review (vs L.6) CONFIRMED (real tables, distinct constants, read-only). cmd_selftest now 10 checks.
  - cycle 21 (2026-08): G.2 harness→production (bible 432→504) via selftest. persona_prompt 3 distinct (zeros>/derez>/zeos>) + cursor_select_colorway 3 distinct accents, restored to FULL default → 'G.2 persona (...): PASS', settled 2-boot. Adversarial review (vs G.1) CONFIRMED (real fns, distinct accents 09BC8A/7A306C/2E86AB, restore-to-2 is true boot default, no lasting change). cmd_selftest now 11 checks.
  - cycle 22 (2026-08): G.3 harness→production (bible 430→505) via selftest. Added read-only persona_lerp() (wraps real color_lerp); cmd_selftest: ZEROS→FULL t=0→src, t=1→dst, t=0.5 mid per-channel between → 'G.3 persona lerp (...): PASS', settled 2-boot. Adversarial review (vs G.2) CONFIRMED (real color_lerp, correct channel math, hard-swap can't false-pass, read-only, spring-transition honestly ceded to harness id=430). cmd_selftest now 12 checks.
  - cycle 23 (2026-08): G.4 harness→production (bible 433→507) via selftest. Un-gated theme_g4_selftest (now returns int, restores scheme); dark ff0d1117/light fffafaf8 distinct+ordered, round-trips, text inverts → '[G4] ...: PASS', settled 2-boot. Adversarial review (vs G.3) CONFIRMED + surfaced that theme_set_scheme PERSISTS to VAULT (3 writes, net-restored — logged in ledger id=507). G-SERIES COMPLETE (G.1-G.5 all production; G.5 was already prod). cmd_selftest now 13 checks.
  - cycle 24 (2026-08): L.5 harness→production (bible 341/415→508) via selftest — RECONCILED a real mismatch (BUILD_MAP said [VERIFIED/production] but ledger had only unscoped KVM selftests). Un-gated context_menu_l5_selftest (returns int); open full+visible, close defers teardown (item_count 2→0 only via real spring-out cb), input off immediately → '[L5] opened=1 input_off=1 deferred=1 torndown=1 -> PASS', settled 2-boot. Adversarial review (vs G.4) CONFIRMED (real fns, no false-pass, no dangling ptr); fixed a stale comment it caught. cmd_selftest now 14 checks.
  - cycle 25 (2026-08): K.3 harness→production (bible 442→509) via selftest. Un-gated sigviz_k3_selftest (returns int); sigviz_tick advances viz.frame +10, sine_pulse(0/15/30)=0/127/255 varies+bounded → '[K3] ...: PASS', settled 2-boot. Adversarial review (vs L.5) CONFIRMED (hand-computed sine_pulse matches, no false-pass, +10 offset wiped by sigviz_init on next open — harmless). cmd_selftest now 15 checks.
- L.4 RESOLVED (cycle 27): the ghost-surface risk is fixed. surface_scale_cb (wm.c:142-153) IS the removal path — it fires on the settling tick because anim.c:165-174 sets active=0 BEFORE the final callback. Fix = tick 600 more after detach so the close spring settles → cb removes it (visible=0/DETACHED). No separate wm per-frame call needed.
  - cycle 26 (2026-08-10): L.3 harness→production (bible 461→510) via selftest. Un-gated compositor_l3_selftest — REWROTE production-safe: replaced the global anim_init() reset (would wipe live springs, same class as L.4's ghost concern) with a scoped anim_cancel(id) on just its own probe spring. cmd_selftest now 16 checks. Settled 2-boot: '[L3] live=1 ticked(pos>0)=1 rearmed=1 -> PASS'; post-selftest workspace-2.png shows desktop composited + Status: Live (no reset side effect). Adversarial review (N-1=K.3, N=L.3) ACCEPT: verified compositor.c:289 re-arm is CONDITIONAL on anim_active_count>0 (not hollow); sigviz overlay closed (:285 didn't fire), no mouse-held (:296) → observed rearmed attributable to the anim gate; 'ticked' (probe value moved past 0) independently proves per-frame anim_tick. Conforms to K.3 paradigm and improved safety over the harness original. commit pending.
  - cycle 27 (2026-08-10): L.4 harness→production (bible 429→511) via selftest — the item DEFERRED since cycle 25 (ghost-surface risk). Un-gated wm_l4_selftest, production-safe (probe persist_name=0 = no VAULT write; synchronous create+spring+teardown = never composited while visible). FIXED the ghost: after detach, tick 600 to full settle so surface_scale_cb removes the surface (visible=0/DETACHED); added a torndown assertion. cmd_selftest now 17 checks; '61 passed' (was 60). Settled 2-boot: '[L4] open[grad/full]=11 close[flag/anim]=11 torndown=1 -> PASS'; evidence/l4-workspace.png clean desktop, no ghost. Adversarial review (N-1=L.3, N=L.4) CONFIRMED: reviewer tried+failed to construct a false-PASS (only surface_scale_cb co-sets all 3 torndown states, only on real settle; anim.c:168-before-172 ordering verified); safety verified in source; slot-consumption caveat honest+pre-existing; no drift. commit pending.
  - cycle 28 (2026-08-10): C.13 harness→production (bible 451→512) via selftest. Un-gated wm_c13_selftest, production-safe like L.4 but x3 surfaces: 3 probes persist_name=0 (no VAULT write), created+asserted+torn-down synchronously (never composited while visible), detach+tick-600-settle so surface_scale_cb removes all 3 (added torndown assert). cmd_selftest now 18 checks; '62 passed' (was 61). wm_snap_adjacent docks chain-77 b to a's right (b.x=400=100+300, matched y/h), unique chain-88 c -> no neighbor. Settled oracle: 2 consistent good boots '[C13] ... adjacent=1 neighbor=1 no_neighbor_for_unique=1 torndown=1 -> PASS' (3rd boot dropped the selftest input entirely = INPUT.DROP id=480, prints NO [C13] line so distinguishable from a fail — retried, not counted). evidence/c13-workspace.png clean, no ghosts. Adversarial review (N-1=L.4, N=C.13) CONFIRMED: asserts non-tautological (b's created geom differs on all 3 axes from snapped result) + self-protecting (create-overflow->NULL->FAIL); chain 77/88 grep-confirmed selftest-only; force_visible->detach still makes a working close spring; safety verified in source+PNG; input-drop dismissal legit; no drift. commit pending.
  - cycle 29 (2026-08-10): C.12 harness→production (bible 454→513) via selftest. Un-gated wm_c12_selftest, production-safe like C.13 (probe persist_name=0 = no VAULT write, created+asserted+torn-down synchronously = no flash, detach+tick-600-settle so surface_scale_cb removes it, torndown assert). cmd_selftest now 19 checks; '63 passed' (was 62). 3 tabs (cpu=10/mem=20/gpu=30), first active renders chain 10, wm_tab_switch(2) rebinds surface->chain_id to 30, OOB switch(9) rejected. Settled oracle: 2 consistent good boots '[C12] count=3 active0=1 chain0=1 switch2=1 chain2=1 reject_oob=1 torndown=1 -> PASS' (2 boots this cycle dropped the selftest input = INPUT.DROP id=480, no [C12] line, retried). evidence/c12-workspace.png clean, no ghost. Adversarial review (N-1=C.13, N=C.12) CONFIRMED: chain0=10->chain2=30 pair load-bearing (only mutation between reads is the switch, stable sf ptr); create-overflow->NULL->FAIL; teardown correct; safety verified in source+PNG; no drift. Noted active0 is weakly satisfiable (create pre-sets active_tab=0) but not load-bearing. commit pending.
  - cycle 30 (2026-08-10): C.14 harness→production (bible 455→514) via selftest. Un-gated wm_c14_selftest, production-safe like C.13 x3 surfaces (persist_name=0, created+asserted+torn-down synchronously, detach-all-3+tick-600-settle, torndown assert). cmd_selftest now 20 checks; '64 passed' (was 63). wm_set_parent links child->parent + grandchild->child; wm_is_ancestor direct (p,c1) AND transitive (p,c2 walks c2->c1->p, 2 hops); self-parent rejected (-1); toplevel parent=-1. Settled oracle: 2 consistent good boots '[C14] link=1 anc_direct=1 anc_transitive=1 self_reject=1 toplevel=1 torndown=1 -> PASS' (boot1 landed first try). evidence/c14-terminal.png shows the LIVE Terminal window rendering the selftest output in-window ending '64 passed' -> zeos> (desktop fully alive, Files+Terminal open), c14-workspace.png clean no ghosts. Adversarial review (N-1=C.12, N=C.14) CONFIRMED: anc_trans genuinely 2-hop (c2->c1->p, not shortcut); create-overflow->wm_is_ancestor(-1,..)=0->FAIL (fail-safe); self_reject returns -1 before mutating; dangling parent_surface refs INERT (grep: read only by wm_get_parent/wm_is_ancestor, never draw/input/focus/z-order); teardown focus re-pick benign; no drift.
- ‼ SLOT-BUDGET WATCH: cmd_selftest now leaks 8 wm surface slots per run (L4 + C13x3 + C12 + C14x3) of WM_MAX_SURFACES=32 (surface_count never decremented on detach = normal wm behavior). Running `selftest` ~4x in ONE boot would exhaust the table -> creates return -1 -> FAIL (fail-safe, never false-pass; per-fresh-boot oracle unaffected). Future multi-surface selftests (C.10 sheet, C.11 popover) add more. If it ever bites: the sweep types selftest once per boot so headroom is fine, but do NOT stack many multi-surface probes without considering the 32 cap.
  - cycle 31 (2026-08-10): C.11 harness→production (bible 452→515) via selftest. Un-gated popover_c11_selftest (ui_context_menu.c) — production-safe with NO teardown loop needed: the popover is a g_pv global, NOT a wm surface, opened+closed synchronously so never composited while active (correct divergence from the C.12-14 surface pattern). Removed redundant mid-file kprint.h include (already at line 255). cmd_selftest now 21 checks; '65 passed' (was 64). popover_open(1850,30) near right edge clamps on-screen (x 1850->1700, 1700+220=1920<=1920), NON-modal (context_menu_active==0), holds 3 lines, closes clean (active=0). Settled oracle: 2 consistent good boots '[C11] opened=1 clamped=1 nonmodal=1 lines_ok=1 closed=1 -> PASS' (boot1 first try). evidence/c11-workspace.png clean, no leftover popover. Adversarial review (N-1=C.14, N=C.11) CONFIRMED: clamped is load-bearing (anchor 1850+220=2070 overshoots 1920 so clamp MUST fire; delete it -> FALSE); lines_ok real content check; g_pv not a wm surface so teardown omission correct; close resets active=0, draw guards on active; include removal safe; no drift. Noted nonmodal is weakest assert (popover never touches g_menu) but not defective.
  - cycle 32 (2026-08-10): C.10 harness→production (bible 453→516) via selftest. Un-gated sheet_c10_selftest (ui_context_menu.c) — production-safe multi-surface: 2 probes (C10parent + C10other) persist_name=0, created+asserted+torn-down synchronously; ADDED teardown of both + ticked the sheet slide-out to settle (slide<=0.01 so sheet_draw early-returns), asserts torndown + sheet_tucked. Removed redundant mid-file kprint.h include. cmd_selftest now 22 checks; '66 passed' (was 65). sheet_open springs down gradually from parent 40px titlebar (SMOOTH: 4t sliding 0<slide<1, 600t down>0.9), MODAL to parent only (sheet_modal_for(p)=1, (other)=0), sheet_close active=0 + animates out. Settled oracle: 2 consistent good boots '[C10] opened=1 modal_parent=1 not_modal_other=1 sliding=1 down=1 closed=1 torndown=1 -> PASS' (boot1 first try; one interleaved boot dropped selftest input = INPUT.DROP id=480, retried). evidence/c10-workspace.png clean, no ghost sheet/surface. Adversarial review (N-1=C.11, N=C.10) CONFIRMED: reviewer SIMULATED the spring (slide~0.016 at 4t = genuine gradual, reduced-motion snap would FAIL sliding); modal discrimination load-bearing; create-overflow doubly fails safe; open spring settles BEFORE close (no dual-spring fight); detached-parent deref safe (active=0 guards); kprint.h removal safe; no drift.
  - cycle 33 (2026-08-10): M.7 harness→production (bible 434→517) via selftest. Un-gated notify_m7_selftest — production-safe, NO surface teardown needed (pure predicate + a persisted config toggle that's saved+restored). cmd_selftest now 23 checks; '67 passed' (was 66). Drives real access_set_focus_mode + notify_focus_suppresses (the EXACT predicate used in the live notify tick at notify.c:814): focus OFF suppresses nothing (00), focus ON suppresses INFO/WARN/ERROR (111) but CRITICAL passes (0). Settled 2-boot (identical): '[M7] off(info/crit)=00 on(info/warn/err/crit)=1110 -> PASS'; evidence/m7-workspace.png healthy desktop. Adversarial review (N-1=C.10, N=M.7) CONFIRMED: CRITICAL-passes distinction load-bearing (suppress-all/suppress-none/ignore-focus all caught); save/restore net-neutral; VAULT-persist (access_save x3, final restores boot value) honestly disclosed = accepted G.4 pattern; predicate is the live one not a test path; no drift. HANDLED VAULT side effect like G.4 (net-restored).
  - cycle 34 (2026-08-10): M.8 harness→production (bible 447→518) via selftest. Un-gated access_m8_selftest — cvd_transform is a PURE function (no VAULT/global/framebuffer), ZERO side effects, no save-restore needed. cmd_selftest now 24 checks; '68 passed' (was 67). CVD_NONE identity; DEUTERANOPIA red 0xFFFF0000->0xFF9FB200, green 0xFF00FF00->0xFF5F4C4C, dist 510->242, alpha preserved. Settled oracle: 2 consistent good boots '[M8] identity=1 red_deut=ff9fb200 grn_deut=ff5f4c4c dist(orig/sim)=510/242 confuses=1 -> PASS' (a 3rd boot dropped selftest input = INPUT.DROP id=480, retried). FENCE preserved: transform+mode verified, full-screen live apply opt-in (perf). Adversarial review (N-1=M.7, N=M.8) CONFIRMED: reviewer hand-recomputed the Vienot matrix (all values exact), purity verified, include-guard safe, no drift.
  - ‼ LESSON (from M.8 review, apply going forward): a selftest's in-code boolean predicate can be LOOSER than the claim (M.8's `confuses` would also pass a degenerate matrix) — what makes it BIBLE-valid is the EXACT VALUES in the grepped serial evidence (ff9fb200/ff5f4c4c/510/242 pin the real matrix). So: always print concrete values (hex/counts/distances) in the [TAG] serial line, not just booleans, and cite those exact values in the ledger evidence. Booleans alone are weak; observed exact values are load-bearing.
  - cycle 35 (2026-08-10): E.2 harness→production (bible 438→519) via selftest. Un-gated cursor_e2_selftest — cursor_set is pure in-memory (no VAULT/dirty); IMPROVED over harness: saves BOTH g_cursor.state AND prev_state up front and restores them exactly (harness left prev_state dirty, which the reviewer confirmed COULD glitch a later E.4 confirm-revert / click-BURST). cmd_selftest now 25 checks; '69 passed' (was 68). CURSOR_COUNT=22, all 22 sprites non-empty 24x24, hotspots per-cursor distinct, cursor_set accepts all. Settled 2-boot (identical): '[E2] states=22 nonempty_sprites=22/22 hotspots_distinct=1 set_ok=1 -> PASS'; evidence/e2-workspace.png clean DEFAULT arrow (restored exactly). Adversarial review (N-1=M.8, N=E.2) CONFIRMED: nonempty=22/22 + states=22 concrete numerics (M.8 lesson satisfied); save/restore a legit improvement not cosmetic; build clean production scope; no drift.
  - cycle 36 (2026-08-10): E.3 harness→production (bible 421→520→521) via selftest. Un-gated cursor_e3_selftest — heavy live-state mutation (cursor_move(500,500), ripples, scale, springs, state). Snapshots whole g_cursor, cancels spawned springs, restores + forces ripples inactive. cmd_selftest now 26 checks; '70 passed' (was 69). SCALE press->0.85+restore, RIPPLE spawns growing/fading ring, BURST swaps+restores. Settled 2-boot: '[E3] scale[press/restore]=11 scale_min_x100=85 ripple[active/grew]=11 r=5 o=137 burst[on/off]=11 -> PASS'; evidence/e3-workspace.png clean arrow, no ghost. Adversarial review (N-1=E.2, N=E.3) REJECTED first pass -> FIXED IN-CYCLE: my cleanup guarded ripple-spring cancels on `id >= 0`, but ripple anim-id fields are ZERO-INITIALIZED (no -1 sentinel) so it fired anim_cancel(slot 0) x6 for the 3 un-spawned ripples, killing an unrelated spring (harmless only by idle coincidence). Fix: guard ripple cancels on `.active` (only spawned ripples) + force ripples inactive after restore (closes the pre-existing-live-ripple ghost case). Also added concrete magnitudes to serial (M.8 lesson the reviewer re-flagged). Re-verified settled 2-boot after fix; re-review requested.
  - ‼ LESSON (E.3): the `id >= 0` guard is WRONG for fields that are zero-initialized without a -1 sentinel (0 is a valid anim slot). Guard spring-cancels on a real liveness flag (.active) or a field with a true -1 sentinel (like anim_scale via cursor_init). When snapshotting+restoring a struct that holds EXTERNAL resource ids (anim slots), cancel the resources the test spawned BEFORE restoring, and force-clear render flags after, so a restored-but-dangling id can't render.
  - cycle 37 (2026-08-10): E.4 harness→production (bible 326->522) via selftest. Un-gated cursor_e4_selftest — production-safe: saves+restores state/prev_state/s_confirm_frames (E.2/E.3 lesson), checkmark never composites. cmd_selftest now 27 checks; '71 passed' (was 70). cursor_confirm() flashes CURSOR_CLICK_CONFIRM(3), cursor_tick reverts to before(0) after CONFIRM_HOLD_FRAMES=18. Settled 2-boot (identical): '[E4] cursor_confirm flash=1 reverted=1 states(before/confirm/after)=0/3/0 -> PASS'; evidence/e4-workspace.png clean arrow. RECONCILED an L.5-class mismatch: BUILD_MAP box already said [VERIFIED/production] but bible id=326 was only a KVM/diag selftest — id=522 supplies real production evidence. Adversarial review (N-1=E.3, N=E.4) CONFIRMED: reverted not tautological (loop 17 vs 18 would FAIL = exact countdown exercised); save/restore net-neutral (s_confirm_frames matters); reconciliation honest; no drift. Non-blocking notes: 3-scalar snapshot (vs E.3 whole-struct) fine at idle; before=0 can't distinguish revert-target but source uses prev_state.
  - cycle 38 (2026-08-10): F.4 harness→production (bible 405->523) via selftest. Un-gated font_f4_selftest — production-safe: read-only over font tables, font_get_glyph's only side effect is an idempotent glyph-cache warm (no save/restore needed). cmd_selftest now 28 checks; '72 passed' (was 71). Finds gap_cp=0x00AD (Inter glyph index 0, Noto !=0), font_get_glyph(FONT_UI) returns the fallback glyph. Settled 2-boot (identical): '[F4] noto_loaded=1 gap_cp=0xad fell_back=1 -> PASS'; evidence/f4-workspace.png real TTF text on live desktop. RECONCILED another L.5-class mismatch (2nd in a row): BUILD_MAP said [VERIFIED/production] but bible id=405 was only a KVM selftest -> id=523 real production. Adversarial review (N-1=E.4, N=F.4) CONFIRMED: fallback branch deterministically taken for 0xAD + FONT_UI cache_render fallthrough unreachable = fell_back necessarily Noto-sourced (not a .notdef false-pass); read-only safe; reconciliation honest; no drift. Non-blocking nit: fell_back checks non-null not provenance (provably fine now; future hardening = assert g->font==FONT_FALLBACK if glyph_t carries source).
  - ‼ PATTERN (E.4 + F.4 back-to-back): several BUILD_MAP boxes read [VERIFIED/production] but are backed by OLD KVM/diag selftest bible entries (not production-scoped) — the L.5 class. When picking an item, if its BUILD_MAP already says production but the bible id is a KVM/pre-selftest-method entry, it's a hidden mismatch to reconcile. Consider a sweep: grep BUILD_MAP for [VERIFIED/production] lines whose bible id < ~467 (pre-loop) and cross-check scope.
  - cycle 39 (2026-08-10): M.4 harness→production (bible 300->524) via selftest. Un-gated access_m4_selftest — production-safe: sets g_access.sensory DIRECTLY (not the persisting/dirtying setter apply_sensory_mode/access_set_sensory) so NO VAULT write + no compositor_dirty, saved+restored (net no change); helpers read-only. cmd_selftest now 29 checks; '73 passed' (was 72). STANDARD passthrough (std==in=ff2e86ab), LOW mutes toward luma (ff4e7a8d)+border1+deco-off, HIGH boosts (ff148fc3)+white text+border2. Settled 2-boot (identical): '[M4] ... std=ff2e86ab low=ff4e7a8d high=ff148fc3 htext=ffffffff border(low/high)=1/2 deco(low)=0 -> PASS'; evidence/m4-workspace.png normal STANDARD colors restored. RECONCILED 3rd consecutive L.5-class mismatch (E.4,F.4,M.4): box said production but bible id=300 was KVM -> id=524. Adversarial review (N-1=F.4, N=M.4) CONFIRMED: reviewer hand-recomputed luma601 (LOW/HIGH both match), all 4 helpers read-only, direct-assign not the persisting setter, honest reconciliation, no drift.
  - cycle 40 (2026-08-10): D.5 harness→production (bible 308->525) via selftest. Un-gated access_d5_selftest — uses the REAL access_set_density (the point: proves end-to-end density->panel-height propagation) + saves/restores density. cmd_selftest now 30 checks; '74 passed' (was 73). BOTH the map (access_get_panel_height table) AND live (panel_get_height = g_panel.height, distinct storage set via panel_set_height inside the setter) == 48/40/32 for COMFORTABLE/STANDARD/COMPACT. Settled 2-boot (identical): '[D5] d=0:map=48,live=48 d=1:map=40,live=40 d=2:map=32,live=32 -> PASS'; evidence/d5-workspace.png panel at normal restored height. VAULT-persist noted (access_save x4, final restores boot value = G.4/M.7). Dropped D.5's redundant kprint.h (unconditional at :121 from M.4). RECONCILED 4th consecutive L.5-class mismatch (E.4,F.4,M.4,D.5): box said production, id=308 was KVM -> id=525. Adversarial review (N-1=M.4, N=D.5) CONFIRMED: map vs live distinct storage (checking both load-bearing, not tautological); VAULT net-neutral+disclosed; no drift.
  - cycle 41 (2026-08-10): J.3 harness→production (bible 336/408->526) via selftest. FIRST skipped G.12 as REDUNDANT (persona_g12_selftest is the OLD combined G.1+G.2 harness test; G.1=id503 + G.2=id504 already production via separate inline checks — no distinct G.12 BUILD_MAP row). Then did J.3: un-gated palette_j3_selftest — production-safe: palette_refresh_settings only mutates the HIDDEN palette item list (never rendered while pal.visible=0, rebuilt on next palette_show; F.4-class benign recompute), doesn't set visible/dirty; snapshot+restore pal.item_count. cmd_selftest now 31 checks; '75 passed' (was 74). settings-category items == settings_count() registry (45==45; grew from old 42). Settled 2-boot (identical): '[J3] palette settings items=45 registry=45 -> PASS'; evidence/j3-workspace.png no palette stuck open. RECONCILED 5th L.5-class mismatch: box said production but bible 336/408 were KVM/observed (BUILD_MAP line 85-86 even documented the old KVM basis). Adversarial review (N-1=D.5, N=J.3) CONFIRMED: enumeration load-bearing, render-gate safety holds, honest reconciliation, no drift. Fixed an inaccurate 'byte-identical' comment the reviewer flagged (real safety = render-gate + rebuild, not byte-identity).
  - ‼ LATENT (J.3, flag for future): palette_j3_selftest's pass has an OR-clause `|| pal.item_count >= PALETTE_MAX_ITEMS(128)` that would report PASS on a TRUNCATED enumeration if settings ever exceed 128 (currently 45, unreachable; palette_refresh_settings prints a truncation NOTE). If settings count approaches 128, tighten this assert to require settings_items==reg.
  - cycle 42 (2026-08-10): J.2 harness→production (bible 395->527) via selftest. Un-gated settings_j2_selftest — cycle_value on the accessibility page routes through the PERSISTING access_set_sensory/reduced_motion/focus_mode setters, so ADDED full save/restore: snapshot page+selected_item + the 3 access fields (s0/r0/f0 already captured as true originals), restore all via the same setters at the end. cmd_selftest now 32 checks; '76 passed' (was 75). shared_ptr=1 (settings_get_access()==access_get()) + all 3 fields change live via the GUI cycler. Settled 2-boot (identical): '[J2] shared_ptr=1 sensory_live=1 reduced_motion_live=1 focus_live=1 s0=0 r0=0 f0=0 -> PASS'; evidence/j2-workspace.png normal STANDARD colors restored. RECONCILED 6th L.5-class mismatch: box said production, id=395 KVM -> id=527. Adversarial review (N-1=J.3, N=J.2) CONFIRMED: asserts load-bearing, cross-field independence verified (s0/r0/f0 uncorrupted), VAULT net-restored+disclosed, no drift. Fixed comment per reviewer re color_temp.
  - ‼ LATENT (J.2, flag for future): access_set_sensory recomputes color_temp from the mode; the restore relies on that (sensory=STANDARD -> color_temp=0) which only holds while night_shift=0 (boot default). If night_shift ever ships ENABLED, the STANDARD branch skips the color_temp reset -> save/restore access_get()->color_temp explicitly. Non-issue on current boot path (night_shift=0, evidence PNG clean).
  - cycle 43 (2026-08-10): J.1 harness→production (bible 437->528->529) via selftest. Un-gated settings_j1_selftest — real VAULT round-trip (save 7/3/2 -> clobber 99 -> load_all reads back from VAULT -> assert; loaded=7/3/2 concrete). cmd_selftest now 33 checks; '77 passed' (was 76). PRODUCTION-SAFE: originals restored + final save_all persists boot values (net VAULT=boot; access untouched). CAUGHT+FIXED a real hazard: save_all() calls cursor_confirm() (E.4 flash) and J.1 calls save_all() TWICE synchronously -> 2 cursor_confirm w/o a tick between corrupts prev_state=CONFIRM -> checkmark would STICK on the live desktop. Fix: drain cursor_tick 24x (>18) then cursor_set(DEFAULT) twice (clears state+prev_state). Settled 2-boot (identical): '[J1] ... mouse_speed=1 key_repeat=1 wallpaper=1 loaded(ms/kr/wp)=7/3/2 -> PASS'; evidence/j1-workspace.png clean arrow, VAULT v1 intact. Adversarial review (N-1=J.2, N=J.1) CONFIRMED: round-trip crosses VAULT boundary (load-fail->keep-99->FAIL), net-restored, cursor hazard real+fixed. Applied its 2 non-blocking notes: added concrete loaded values to serial (M.8) + double cursor_set for byte-clean cursor (id=529 supersedes 528).
  - ‼ LESSON (J.1): a selftest that calls a real action twice may re-trigger a UI side effect (here save_all->cursor_confirm) whose OWN state machine assumes spacing (cursor_tick between). Two synchronous triggers corrupted prev_state. When un-gating a selftest that invokes a real user-action helper, check what ELSE that helper fires (cursor_confirm, notify, compositor_dirty) and neutralize/restore it.
  - cycle 44 (2026-08-10): J.4 harness→production (bible 449->530) via selftest. Un-gated settings_j4_selftest — settings_open_page opens the Settings WM app on INPUT(1) + re-routes to A11Y(2) while open. cmd_selftest now 34 checks; '78 passed' (was 77). PRODUCTION-SAFE (applied both J.1 + C-series lessons): settings_open() creates a real WM surface, settings_close() detaches it (springs closed) -> after final close, tick anim 600 to settle so surface_scale_cb removes it (NO ghost Settings panel); drained the save_all confirm-cursor flash (cursor_tick + cursor_set(0)x2); snapshot+restore page/selected_item/scroll_y; VAULT net-neutral (save_all writes unchanged current values). Settled 2-boot (identical): '[J4] open_on_input=1 reroute_to_a11y=1 page(input/a11y)=1/2 -> PASS'; evidence/j4-workspace.png NO ghost panel mid-screen, clean arrow, VAULT v1. RECONCILED 7th L.5-class mismatch (id=449 measured/harness -> 530). Adversarial review (N-1=J.1, N=J.4) CONFIRMED: teardown crux verified at source (close spring holds ctx ptr not id, removed on tick 1 despite surface_id=-1), single confirm drained, VAULT net-neutral, enum 1/2 match, no drift.
  - cycle 45 (2026-08-10): C.4 harness→production (bible 410->531) via selftest. Un-gated wm_c4_selftest — PURE geometry over a STACK-LOCAL chain_surface_t (no wm surface, no g_*/VAULT/cursor/compositor, zero side effects, no save/restore). cmd_selftest now 35 checks; '79 passed' (was 78). Real static hit_resize_edge, 8px band: corner=6(4|2) right=2 left=8 top=1 center=0 12px-in=0 (in12 sits 4px outside the band on both axes = tight boundary discriminator). Settled 2-boot (identical): '[C4] corner=6 right=2 left=8 top=1 center=0 in12=0 -> PASS'. RECONCILED 8th L.5-class mismatch (id=410 KVM -> 531). Adversarial review (N-1=J.4, N=C.4) CONFIRMED: reviewer hand-recomputed all 6 probe geometries (all match), hit_resize_edge pure, in12/center load-bearing negatives, byte-zero sound, no drift.
  - cycle 46 (2026-08-10): M.5 harness→production (bible 301->532) via selftest. Un-gated wm_m5_selftest — same clean class as C.4: PURE geometry over a STACK-LOCAL surface + real static hit_control; only g_wm.controls_side touched, saved+restored. cmd_selftest now 36 checks; '80 passed' (was 79). min_touch_target=44; glyph/hslop@+7(dx18)/vslop hit button 3; beyond@+19(dx30)/offbar miss -> the hslop-hit + beyond-miss bracket 18<=catch_half<30 (load-bearing in BOTH directions: no-expansion AND over-expansion regressions both FAIL). Settled 2-boot (identical): '[M5] min_touch_target=44 glyph=3 hslop@+7=3 vslop(top)=3 beyond@+19=miss offbar=miss -> PASS'. RECONCILED 9th L.5-class mismatch (id=301 KVM -> 532). Adversarial review (N-1=C.4, N=M.5) CONFIRMED: hand-recomputed geometry (catch_half=22, band x[443,487]), all 6 subasserts match, hit_control side-effect-free, net-neutral, no drift. Nits: vslop tests whole-titlebar (honestly labeled), exact boundary bracketed not pinned.
  - cycle 47 (2026-08-10): G.5 harness→production (bible 445->533->534) via selftest. Un-gated dock_g5_selftest — snapshots+restores the WHOLE g_dock struct (dock_state_t value type, inline name buffers) around 3 dock_apply_persona_defaults calls (L.3/E.2 whole-struct pattern). cmd_selftest now 37 checks; '81 passed' (was 80). Distinct per-persona launcher sets. Settled 2-boot (identical): '[G5] zeros[2]=Build derez[1]=Editor derez[3]=Chains full[4]=Calculator counts=5/5/5 -> PASS'; evidence/g5-workspace.png dock restored to boot set. Adversarial review (N-1=M.5, N=G.5) CONFIRMED: snapshot/restore sound (value struct, no heap ptrs), Build[2]/Calculator[4] per-persona unique. STRENGTHENED per its note: added Chains[3] (DereZ-unique) so the Build/Chains/Calculator triad distinguishes ALL 3 personas (Editor[1] alone was shared with Full[1]) — id=534 supersedes 533.
  - ‼ LESSON (G.5 review): a distinctness assert must use a discriminator UNIQUE to each option, not one shared with a sibling. Editor[1] proved DereZ!=Zeros but not DereZ!=Full. When asserting N-way distinctness, pick a per-option-unique field for each, or a regression collapsing two options passes silently.
  - cycle 48 (2026-08-10): D.13 harness→production (bible 465->535) via selftest. Un-gated dock_d13_selftest — mutates BOTH access density (persisting setter, VAULT) AND rewrites g_dock pins (pin/reorder/poof). Applied G.5+D.5 patterns: save density + snapshot whole g_dock; restore density via access_set_density (net VAULT + re-layout + s_dock_item_px) THEN g_dock=saved_dock (after, so dock_apply_density's dock_w is overwritten). cmd_selftest now 38 checks; '82 passed' (was 81). density 68/60/52 (big>std>small exact), reorder A,B,C->B,C,A, poof->B,A. Settled 2-boot (identical): '[D13] density(big/std/small)=68/60/52 reorder=1(BCA) poof=1(BA) -> PASS'; evidence/d13-workspace.png dock restored to boot set+size, panel normal. hover-thumbnail N/I fenced honestly. Adversarial review (N-1=G.5, N=D.13) CONFIRMED: constants match, restore order correct (density then g_dock), POD struct byte-copy valid, no dirty globals, VAULT net-restored, no drift.
  - cycle 49 (2026-08-10): D.6 harness→production (bible 444->536) via selftest. Un-gated panel_d6_selftest — pure in-memory auto-hide state machine (panel_set_auto_hide + panel_pointer_y touch only g_panel.auto_hide/.visible), both fields saved+restored. cmd_selftest now 39 checks; '83 passed' (was 82). enable->hide, top-edge->reveal, below-bar->hide, disable->show (visible seq 0/1/0/1). Settled 2-boot (identical): '[D6] hide_on_enable=1 reveal_top=1 hide_below=1 show_on_disable=1 visible_seq=0101 -> PASS'; evidence/d6-workspace.png panel visible at top (restored). Vibrancy + per-pill (id=416) honestly fenced (not in this selftest). Adversarial review (N-1=D.13, N=D.6) CONFIRMED: all 4 transitions load-bearing (both pointer_y probes genuinely flip visible; no-op FAILs), pure in-memory, net-restored, fencing honest, no drift.
  - cycle 50 (2026-08-10): N.1 harness/KVM→production (bible 304->537->538) — a BOOT-FLOW item (not a cmd_selftest check). Un-gated the 5 [N1] screen prints in firstboot.c (pure serial, no behavior change) so the production cold-boot shows the full 5-screen progression. VERIFIED on the real cold-boot path: fresh-vault sweep boot -> PIN enrollment -> [N1] screen 1/5..5/5 -> [N1] first-run applied: persona=2 controls=0 theme=1 density=1 -> desktop; 2 rendered wizard screens (n1-wiz-1 persona, n1-wiz-3 appearance). Settled 2-boot. IDEMPOTENT SKIP proven on production via a reused-vault reboot (scratchpad n1_idem.py): PIN required (unlock) -> '[main] first-run: already complete, skipped' + no [N1] lines -> desktop. RECONCILED 10th L.5-class mismatch (id=304 KVM). Adversarial review (N-1=D.6, N=N.1) CONFIRMED: block runs in plain build, un-gate purely additive, screenshots real, idempotency mechanism sound + differential valid. Strengthened per its 2 notes: added the affirmative skip marker to evidence + noted the defaults-only value caveat honestly.
  - ‼ N.1 NOTE: idempotency required a REUSED-vault reboot (the sweep wipes the vault each run so it can never test the skip). Wrote scratchpad n1_idem.py (reuse vault + unlock PIN + grep for skip). Pattern for any 'persistence across boots' claim: the sweep's fresh-vault-per-run means cross-boot persistence must be tested by a separate reused-vault boot.
  - cycle 51 (2026-08-10): C.9 harness/KVM→production (bible 413->539). C.9 had NO gated selftest (only a render hook), so WROTE a new wm_c9_selftest (like M.5): stack-local surface + real wm_set_controls_side + real hit_control. Proves controls_side relocates the buttons: LEFT->button on left edge (miss right), RIGHT->mirror. FAITHFUL VISUAL proxy: the draw loop (wm.c ~1161) uses the character-identical btn_x formula as hit_control (wm.c ~802) — buttons drawn where hit-tested. cmd_selftest now 40 checks; '84 passed' (was 83). Fixed the -1-as-4294967295 serial print -> 'miss'. Settled 2-boot (identical): '[C9] LEFT.l=0 LEFT.r=miss RIGHT.l=miss RIGHT.r=3 -> PASS'. RECONCILED 11th L.5-class mismatch (id=413 KVM screenshot); pixel-render fenced to id=413. Adversarial review (N-1=N.1, N=C.9) CONFIRMED: 244px-vs-22px separation (11x margin), all 4 probes load-bearing, draw/hit formulas identical, pure in-memory net-restored, new-selftest legit, scope correct.
  - cycle 52 (2026-08-10): B.4 harness→production (bible 436->540->541) — a [high] harness PROVISIONAL, not a selftest. Un-gated the B.4 smoke-check embedded in compositor_advance (removed 3 ZEOS_DIAG_B4_FRAMEDT_SELFTEST wrappers) — self-terminates after 3 frames, ~free after. Confirms frame_dt is TSC-derived on the plain cold-boot path. Settled 2-boot: '[compositor] B.4 smoke-check: summed frame_dt=107774us vs raw-TSC delta=107774us (fallback ~49998us) -> on TSC path' / boot2 108217us. evidence/b4-workspace.png composited desktop. Adversarial review (N-1=C.9, N=B.4) CONFIRMED contingent on fixing stale BUILD_MAP lines (106/118 listed B.4 as compiled-out — FIXED). Review sharpened the honest scope: the sum==real diff is a telescoping identity (structurally ~0); the REAL discriminator is (a) the line EMITS only when freq>0 (same gate as the TSC branch) + (b) real_us~36ms/frame (TSC calibrated). Sharpened the comment + ledger + BUILD_MAP accordingly.
  - cycle 55 (2026-08-10): B.1 [medium] no-scope→production (bible 10->546->547) — a [medium] VERIFIED with provenance but NO declared scope (A.4-defect class). NO kernel change — pure production observation reconciling scope. B.1 "Compositor init 1920x1080@60". Resolution is GOP-DERIVED (single-source-traced main.c:384 init_gop -> 436 fb_init -> fb.c:326/331 fb_width/height -> main.c:1228 compositor_init_ex(fb_width(),fb_height(),0)), NOT a hardcode; compositor logs its own args (compositor.c:160-165). Settled 2-boot (byte-identical init serial): GOP 'Framebuffer: 1920x1080 pitch=1920', 'COMP: initialized 1920x1080 @ 60fps', 'COMP: double buffer active', '[main] graphical desktop shell up' (fires only after real desktop_draw/panel_draw/dock_draw/wm_draw_all, main.c:1418-1422). evidence/b1-desktop{,2}.png BOTH 1920x1080 composited multi-surface (overlapping z-ordered Files+Terminal, panel+clock, dock, cursor; differ only in live clock 17:53/17:55 = independent captures, not static). HONEST SCOPE: '@60'=target_fps (compositor.c:142, pacing target not measured refresh; frame_dt TSC-derived per B.4 id=541). Adversarial review (N-1=D.2, N=B.1) CONFIRMED 5/5: resolution genuinely detected (GOP+COMP share one struct), logged-but-dead ruled out (shell-up after real draws + genuine composite), plain make all no diag, @60 fence honest, supersede of no-scope id=10 appropriate. Sharpened per its cosmetic note (precise serial-line attribution) in id=547.
  - cycle 54 (2026-08-10): D.2 [medium] no-scope→production (bible 50->544->545) — a [medium] VERIFIED with provenance but NO declared scope (the A.4-defect class). NO kernel change — pure production observation reconciling scope. Panel clock reads tod_now_unix() (CMOS RTC), NOT the TSC-uptime fallback. Settled 2-boot oracle vs host UTC: boot1 serial 'Wall clock ............ 2026-08-10 17:32:17 UTC (CMOS read OK)' panel=17:32; boot2 '...17:34:08 UTC (CMOS read OK)' panel=17:34 — clock ADVANCED ~2min tracking host (17:31->17:35). KILLER PROOF live-not-static/uptime: boot2 is a fresh cold boot (TSC reset toward 0) yet reads LATER than boot1 — only a live RTC re-read yields boot2>boot1. evidence/d2-workspace{,2}.png. Adversarial review (N-1=PALETTE.INPUT, N=D.2) CONFIRMED 5/5: serial 'CMOS read OK' and panel share the same s_ready/s_source globals (no divergent panel->TSC path); plain make all, no diag flags (grep -c=0), clock code unconditional; superseding id=50 (no-scope) appropriate. Sharpened per its honest nuance: displayed HH:MM = CMOS-epoch anchor + TSC intra-boot advance (still the CMOS path, not the fallback) — disclosed in id=545.
  - cycle 53 (2026-08-10): PALETTE.INPUT harness→production (bible 425->542->543) — a [high] provisional, NO kernel change (pure production observation of the already-committed id=425 fix). Added a permanent sweep_boot.py step: after the reliable TL-hot-corner palette open, type 'mem' with real i8042 keystrokes + capture. evidence/paletteinput-typed.png shows the palette filtered to 3 memory results; serial '[sched] preempted chain 14 palette after ~496152us' -> the palette chain preempts yet input still works = the sti-on-longjmp IRQ-survival fix live on production. Settled 2-boot. Super+Space was UNRELIABLE (meta dropped -> plain Space = Quick Look); TL hot corner (mouse) is the reliable palette-open. Adversarial review (N-1=B.4, N=PALETTE.INPUT) CONFIRMED: screenshot is the real filtered palette, regression would be caught (IF=0 kills ALL input before 'mem'), scope honest. Sharpened proof basis to WHOLE-SWEEP IRQ liveness (kbd echoes selftest + mouse fires later hot corners post-preempt), not the isolated [sched] line.
  - ‼ LESSON (PALETTE.INPUT): a combo (Super+Space) for a MODAL open is INPUT.DROP-prone (meta drops -> the bare key does something else, e.g. Space->Quick Look). Prefer a mouse-driven open (hot corner) for modal-overlay tests. And for an IRQ-liveness claim, the proof is system-wide liveness across the event (kbd+mouse both alive after), not one serial line.
  - ‼ LESSON (B.4 review): an un-gate can leave STALE doc elsewhere — BUILD_MAP had 2 lines still calling B.4 'compiled-out'. When un-gating a diag block, grep BUILD_MAP/docs for the item + 'compiled-out'/'diag build' and reconcile. Also: a self-comparison (sum vs a value derived from the same source) is a telescoping identity — cite the STRUCTURAL discriminator (does the code path even run? is the input sane?), not the tautological match.
- STATUS: RUNNING. selftest 40 checks + N.1/B.4 boot-flow + PALETTE.INPUT; Terminal real; G-series(+G.5) + K.3 + L.3/L.4 + C.4/C.9/C.10-C.14 + M.4/M.5/M.7/M.8 + E.2/E.3/E.4 + F.4 + B.4 + D.5/D.6/D.13 + J.1/J.2/J.3/J.4 + N.1 + PALETTE.INPUT done. 11 L.5-class reconciliations. B.4 + PALETTE.INPUT harness-provisionals CLEARED. REMAINING harness [high] provisionals: A.4 (scheduler preempt) + A.6 (composite region-dirty / tick-overrun) — BOTH the DELICATE preempt/IRQ/tick cluster with a DOCUMENTED OPEN spin-hang history (A.9: real-build spin-hang the same preempt watchdog didn't catch; A.4 id=32 was a false-VERIFIED, downgraded). A.4's selftest DELIBERATELY hangs a chain to test the watchdog rescue -> risky to trigger on the production sweep (could hang the boot). Before attempting A.4/A.6: check if A.9 is resolved; if still open, these need Brad's call (STOP + STATE note) rather than forcing a green. Plus 11 [medium] NO-declared-scope VERIFIEDs (A.1/A.2/A.3/A.5, B.2/B.6, E.6, M.1/M.2/M.3/M.6) — likely already production-behavior, need re-verify + scope tag (SAFE, cheaper). [D.2 DONE cycle 54 → prod id=545. B.1 DONE cycle 55 → prod id=547.] B.9 (per-frame checksum harness), B.5. NEXT: prefer a [medium] no-scope item (safe) or assess A.9 status before A.4/A.6. REMAINING harder items: C.9 (controls side L/R — NO gated selftest fn, only an inline render hook; needs a visible-window left-buttons screenshot = render/click item, box already production id=413 KVM — either build a real render check or leave as documented render item), B.9 (partial redraw — ZEOS_DIAG_B9_SELFCHECK is a per-frame checksum harness in compositor.c, not a shell-callable fn; box already production, complex), N.1 (5-screen first-boot, ALREADY driven by the sweep wizard each boot = arguably already production-observed; could ledger from the wizard screens), then surface-teardown gated selftests: D.13 (dock_d13 reorder/poof), D.6 (panel_d6 auto-hide/vibrancy), G.5 (dock_g5), plus any remaining *_selftest fns (grep). B.4 (frame_dt TSC) + A.4 measured-not-selftest. Rebuild list from `bible.py lint zeos` (20 scope issues) + grep '_selftest' for un-wired gated fns. REMINDER: multi-surface/WM-surface selftests MUST tear down. SLOT-BUDGET WATCH: ~11 wm slots/run of 32. RULES: (M.8) print exact values in serial + cite; (E.2) save+restore ALL touched state; (E.3) guard resource-id cancels on liveness flag/real -1 sentinel; (M.4/M.7) direct non-persisting mutation else note VAULT-persist+save/restore; (J.1) check what a real-action helper ALSO fires and neutralize/settle it; verify serial-item is a REAL distinct BUILD_MAP row before spending a cycle; watch for L.5-class mismatches.
